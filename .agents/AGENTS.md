# Custom Rules

- **Git Commit Constraint**: DO NOT perform `git commit` commands automatically on behalf of the user. Only modify workspace files, stage them if requested, but always leave git commit operations to the user's manual action.
